# 自适应定投与再平衡研究

这部分是实验层，不替换 `backtest.py` 的基线策略。

## 为什么做这一层

`25/25/25/25` 只是一个方便运行的基准，不是预设答案。真正需要同时研究的是：

1. 四只 ETF 各自应该占多少；
2. 每月新资金是机械按目标权重买，还是优先补低配资产；
3. 全组合卖出再平衡应该多久一次；
4. 是否应该从“固定日历”改成“偏离阈值”触发。

## 自适应定投

`adaptive_strategy.py` 支持两种 contribution mode：

- `target`：新资金按目标权重拆分；
- `underweight`：新资金优先买当前低配资产，普通月不卖出。

`underweight` 的目标不是追涨杀跌，而是尽量让新增现金承担再平衡任务，从而减少不必要的卖出、换手和手续费。

## 全组合再平衡规则

支持：

- `periodic`：固定每 N 个定投月全组合再平衡；
- `threshold`：月度定投完成后，如果任一资产相对目标权重偏离超过阈值，再做全组合再平衡；
- `either`：周期或阈值任一满足就触发；
- `none`：只靠新增资金纠偏，不做卖出式全组合再平衡。

阈值检查使用**已投资资产权重**，排除整手约束造成的残余现金，避免现金本身制造虚假偏离信号。

## 联合搜索

运行：

```bash
python policy_sweep.py
```

默认权重空间：

```text
单只最低 10%
单只最高 50%
步长 5%
完整四 ETF 网格 375 组
```

默认不是围绕 25% 做局部搜索。流程分两阶段：

1. 在完整 375 组网格上，分别用 `target` 和 `underweight` + 3 个月再平衡寻找稳健权重区域；
2. 合并两边 Top 权重候选，再比较不同资金规则和再平衡规则。

默认比较：

```text
定投方式：target / underweight
固定周期：1 / 3 / 6 / 12 个月
偏离阈值：3% / 5% / 10%
以及：完全不做卖出式再平衡
```

等权 `25/25/25/25` 仅作为 benchmark，只在它恰好属于搜索网格时保留一个对照行。

## 排名原则

最终评分仍以收益和稳健性为主：

- 全周期 XIRR；
- Sharpe；
- 最大回撤；
- 最差分段 XIRR；
- 分段平均 XIRR；
- 分段稳定性；
- 权重集中度。

在此基础上仅给少量附加权重给：

- 更低手续费；
- 更低平均权重偏离。

手续费不会被赋予过高权重，因为交易成本已经实际扣进回测收益；这里的额外成本排名只用于打破表现接近的候选。

## 输出

```text
adaptive_seed_weight_rankings.csv
adaptive_policy_results.csv
adaptive_policy_top.csv
adaptive_policy_summary.csv
adaptive_top_region.csv
```

其中：

- `adaptive_seed_weight_rankings.csv`：完整权重网格在两种定投模式下的初筛结果；
- `adaptive_policy_results.csv`：候选权重 × 所有策略规则的完整结果；
- `adaptive_policy_top.csv`：联合 Top 候选；
- `adaptive_policy_summary.csv`：把权重差异平均掉后，看哪类执行规则整体更稳；
- `adaptive_top_region.csv`：Top 区域里每只 ETF 权重的均值、范围、标准差，以及 underweight DCA 的出现比例。

## 如何判断结果可信

不要只看第一名。更值得采用的是以下情形：

- Top 区域里某只 ETF 权重集中在较窄区间，而不是 10% 到 50% 到处跳；
- `underweight` 或某个再平衡规则在多数优质权重下都表现不错；
- 换成 3/6/12 个月后结论没有彻底反转；
- 3%/5%/10% 阈值中至少有一片稳定区，而不是只有单个阈值异常优秀；
- 后续 Walk-Forward 样本外仍能复现相同方向。

如果 Top 权重和策略规则高度不稳定，正确结论不是继续把步长细化到 1%，而是承认当前历史样本不足以识别精确比例。
