# 自适应定投与再平衡研究

这部分是实验层，不替换 `backtest.py` 的基线策略。

## 研究问题

当前默认 ETF universe 为五只：

```text
513180.SH  恒生科技
515450.SH  红利低波
513300.SH  纳斯达克100
159783.SZ  科创创业50
518850.SH  华夏黄金ETF
```

`20/20/20/20/20` 只是快速运行 benchmark，不是预设答案。真正需要同时研究：

1. 五只 ETF 各占多少；
2. 黄金作为独立防御 / 实物资产因子应该占多少；
3. 每月新资金机械按目标权重买，还是优先补低配资产；
4. 全组合卖出再平衡多久一次；
5. 是否应该从固定日历改成偏离阈值触发。

## 自适应定投

`adaptive_strategy.py` 支持：

- `target`：新资金按目标权重拆分；
- `underweight`：新资金优先买低配资产，普通月不卖出。

`underweight` 的目标是让新增现金尽量承担再平衡任务，从而减少不必要卖出、换手和手续费。

## 全组合再平衡规则

实验引擎支持：

- `periodic`：固定每 N 个定投月全组合再平衡；
- `threshold`：月度定投完成后，资产权重偏离超过阈值才再平衡；
- `either`：周期或阈值任一满足即触发；
- `none`：只靠新增资金纠偏，不做卖出式全组合再平衡。

阈值检查使用**已投资资产权重**，排除整手约束造成的残余现金，避免现金本身制造虚假偏离。

## 联合搜索

运行：

```bash
python policy_sweep.py
```

默认权重空间：

```text
单只最低 10%
单只最高 50%
步长 5%
完整权重网格 976 组
```

默认执行规则：

```text
定投方式：target / underweight
固定周期：1 / 3 / 6 / 12 个月
偏离阈值：3% / 5% / 10%
以及：完全不做卖出式再平衡
```

共 16 种执行规则。

### 阶段 1：完整联合筛选

不是先用某一种策略筛权重，而是直接跑：

```text
976 组权重 × 16 种执行规则 = 15,616 组完整历史回测
```

每一种执行规则都独立保留自己的 Top 权重候选，所以不会出现“3 个月再平衡先把只适合 12 个月策略的权重淘汰掉”的问题。

默认每种规则保留 15 组进入下一阶段：

```bash
python policy_sweep.py --screen-top-per-policy 15
```

### 阶段 2：分段稳健性验证

对阶段 1 每种规则自己的 Top 候选，再跑连续时间分段，加入：

- 最差分段 XIRR；
- 分段平均 XIRR；
- 分段 XIRR 标准差；
- 最差分段最大回撤。

最终评分仍以收益与稳健性为主，只给少量权重给更低手续费和更小平均权重偏离。

## 等权 benchmark 怎么处理

`20/20/20/20/20` 在 5% 网格中天然就是 976 组候选之一，所以无需特殊插入或围绕它搜索。

它会有一个 `is_equal_weight_benchmark` 标记，方便后续对照，但不会影响权重搜索中心。

## 输出

```text
adaptive_joint_screen.csv      # 976 权重 × 全部执行规则的完整历史筛选
adaptive_policy_results.csv    # 进入分段验证后的稳健排名
adaptive_policy_top.csv        # 最终 Top 区域
adaptive_policy_summary.csv    # 各执行规则自己的候选稳健性
adaptive_top_region.csv        # Top 区域权重范围和执行规则分布
```

## 如何判断结果可信

不要只看第一名。更值得采用的是：

- Top 区域里某只 ETF 权重集中在较窄区间，而不是 10% 到 50% 乱跳；
- 黄金的目标权重在不同时间段和执行规则下仍落在相近区间；
- 某个执行规则在自己的一批优秀权重下都表现好，而不是只靠一组特殊比例；
- 1/3/6/12 个月的小变化不会让结论彻底反转；
- 3%/5%/10% 阈值中存在稳定区域；
- `underweight` 的优势不是只来自某一个历史阶段；
- 后续 Walk-Forward 样本外仍能复现方向。

如果 Top 权重和策略规则高度不稳定，正确结论不是继续把步长细化到 1%，而是承认当前历史样本不足以识别精确比例。
